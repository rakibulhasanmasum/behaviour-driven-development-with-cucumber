---
- hosts: all
  tasks:
#    - name: Get user name
#      shell: whoami
#      register: foo
#    - debug:
#        msg: "{{ foo.stdout }}"

    - name: copy files to nginx server
      copy:
        src: "target/generated-report"
        dest: /usr/share/nginx/html
        owner: shepherd
        group: shepherd
